Running the Project
===================

To run the full project:

.. code-block:: bash

    PYTHONPATH=src python src/main.py

This ensures Python treats the `src/` directory as the root
of the project.